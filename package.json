{
  "name": "geo-ca",
  "version": "0.0.0",
  "description": "A monorepo containing the geo.ca web application and related services.",
  "main": " ",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
		"sst:deploy:dev": "sst deploy --stage dev",
		"sst:deploy:stage": "sst deploy --stage stage",
		"sst:deploy:prod": "sst deploy --stage prod",
		"sst:dev": "sst dev --stage dev"
  },
  "author": "Pascal Vautour",
  "license": "ISC",
  "dependencies": {
    "sst": "^2.36.3"
  }
}
